const prompt = require("prompt-sync")({ sigint: true });

const array = [];

const valor1 = parseInt(prompt("Digite um número: "));
const valor2 = parseInt(prompt("Digite um número: "));
const valor3 = parseInt(prompt("Digite um número: "));

array.push(valor1, valor2, valor3);

console.log(array);

array.splice

// Preenchimento de array
const arr = new Array(10);
arr.fill("x");

// Concatenação de arrays
const arr1 = [1,2,3];
const arr2 = [4,5,6];
const arr3 = arr1.concat(aar2);

// Reversão de Arrays (altera o array original)
const arr = [1,2,3,4];
arr.reverse();

// Ordenação Arrays
const arr = [9,2,7,4];
const arr = ["zambia", "andre", "cris", "maria"];
arr.sort()

/* Link de estudo de métodos do Array
https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Global_Objects/Array
*/