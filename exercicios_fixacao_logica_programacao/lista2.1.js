/*
Faça um algoritmo que leia os valores A, B, C e imprima na tela se a soma de A + B é menor que C.
*/

const a = 1;

const b = 2;

const c = 5;

(a + b < c) ? console.log(`A soma de A + B é menor que C.`) : console.log(`A soma de A + B não é menor que C.`)